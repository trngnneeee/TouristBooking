mixin options(list, level, parent="")
  each item in list
    option(
      value=item.id 
      selected=(parent == item.id)
    )= `${"--".repeat(level)} ${item.name}`
    if item.children && item.children.length > 0
      +options(item.children, level + 1, parent)